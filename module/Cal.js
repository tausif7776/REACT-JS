let add = (num1, num2) => {
    return num1 + num2;
}
let subtract = (num1, num2) => {
    return num1 - num2;
}
let multiply = (num1, num2) => {
    return num1 * num2;
}
let divide = (num1, num2) => {
    return num1 / num2;
}

export const arr = ["Ravi", "Ganesh", "Ramesh", "Suresh"];

export {add, subtract, multiply, divide};

// export default add;